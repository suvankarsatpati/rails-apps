<h1>Daily Tasks List</h1>


<%= form_for Task.new do |f| %>
	<%= f.label :task %>
	<%= f.text_field :task %>
	<%= f.submit %>
<% end %>

<ul>
	<% for task in @tasks %>
		<li>
			<%= task.task %>
		</li>
	<% end %>
</ul>
